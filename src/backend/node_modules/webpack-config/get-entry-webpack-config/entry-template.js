/* eslint-disable */
/**
 * @file: entry of client
 * @author: Cuttle Cong
 * @date: 2018/1/17
 * @description:
 */

function start() {
    <% for (let i = 0; i < entries.length; i++) { %>
    require('<%= entries[i] %>');
    <%}%>
}

if (module.hot) {
    module.hot.accept();
}

start();
